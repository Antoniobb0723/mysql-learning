This is the beginner series of me learning SQL. It includes select, from, where, group by, having/limit, order by, and logical operators (and or not)
The second file is intermediate SQL. It includes joins, unions, string functions, case statements, sub queries, window functions
